{
  "name": "shifttokens-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && mv dist ../dist",
    "start": "cd backend && npm install && node server.js"
  }
}
